const defaultTheme = require('tailwindcss/defaultTheme')

module.exports = {
  content: ['./src/**/*.{html,tsx,ts}'],
  theme: {
    container: {
      padding: '2rem',
    },
  },
  variants: {},
}
